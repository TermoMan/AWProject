<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
      crossorigin="anonymous"
    />
    <script type="text/javascript">
      function onSubmit(){
        return confirm("¿Estás seguro de que quieres eliminar esta coleccion?");
      }
    </script>
    <link rel="stylesheet" type="text/css" href="/css/list.css" />
    <!-- <link rel="shortcut icon" type=”image/png” href="/img/favicon.png"/> -->
    <title>Collectio - Colecciones</title>
  </head>
<body class="bg-dark">
  <%- include('header', { pageName: 'collections'}) %>
  <main>
    <div class="container-md">
      <h2>Colecciones</h2>
      <div class="row my-5">
        <div class="col d-flex justify-content-between">
          <a href="/collections/create" class="btn btn-primary" id="addC">Añadir colección</a>
        </div>
      </div>
      <div class="row">
        <div class="col">
          <% if (exist) { %>
            <% if (collectionsList.length==0) { %>
              <div class="alert alert-dark" role="alert">
                No tienes coleccciones. Si quieres añadir una, pulsa sobre el botón "Añadir colección".
              </div>
              <% } else { %>
                <div class="table-responsive">
                  <table class="table table-dark table-striped table-hover items-list">
                    <thead>
                      <tr>
                        <th>Nombre</th>
                        <th>Descripción</th>
                         <th>Privada</th>
                        <th>Imagen</th>
                        <th>Opciones</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% collectionsList.forEach(function(collection) { %>
                        <tr>
                          <td>
                            <a href="/collections/<%= collection.id %>" ><%= collection.name %></a>
                          </td>
                          <% if (collection.description) { %>
                            <td>
                              <%= collection.description %>
                            </td>
                            <% } else { %>
                              <td> Sin descripción </td>
                              <% } %>
                              <% if (collection.privat == 1) { %>
                                <td>
                                  Sí
                                </td>
                                <% } else { %>
                                  <td> No </td>
                                  <% } %>
                                <% if (collection.image==null) { %>
                                  <td>Sin imagen</td>
                                  <% } else { %>
                                    <td><img src="/collections/getImage/<%=collection.image%>"></td>
                                    <% } %>
                                      <td>
                                        <form onSubmit="onSubmit()" action="/collections/deleteCollection/<%=collection.id%>" method="POST">
                                          <input class="btn btn-danger btn-sm collection-delete" name="delete" type="submit" value="Eliminar">
                                        </form>
                                        
                                      </td>
                                    </tr>
                        <% }); %>
                    </tbody>
                  </table>
                </div>
              <% } %>
            <% }else { %>
                <div class="alert alert-dark" role="alert">
                  No tienes acceso a esta coleccion.
                </div>
            <% } %>
        </div>
      </div>
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf"
    crossorigin="anonymous"></script>
</body>

</html>